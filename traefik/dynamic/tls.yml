tls:
  certificates:
    - certFile: /etc/traefik/certs/localhost+2.pem
      keyFile: /etc/traefik/certs/localhost+2-key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/certs/localhost+2.pem
        keyFile: /etc/traefik/certs/localhost+2-key.pem