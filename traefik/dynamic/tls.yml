tls:
  certificates:
    - certFile: /etc/traefik/certs/localhost+5.pem
      keyFile: /etc/traefik/certs/localhost+5-key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/certs/localhost+5.pem
        keyFile: /etc/traefik/certs/localhost+5-key.pem