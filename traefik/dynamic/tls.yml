tls:
  certificates:
    - certFile: /etc/traefik/certs/localhost+7.pem
      keyFile: /etc/traefik/certs/localhost+7-key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/certs/localhost+7.pem
        keyFile: /etc/traefik/certs/localhost+7-key.pem